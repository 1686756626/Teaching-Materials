# Teaching Materials — AI Agent Guide

> 一对一教培备课资料仓库，存储所有学生各周的教学材料。
> **任何修改后必须同步更新 Teaching-System/AGENTS.md 的变更日志。**

## 系统定位

本仓库是教学备课系统的**数据输出仓库**，AI 备课助手按规范生成的所有材料都写入这里。
配套仓库见 [Teaching-System/AGENTS.md](https://github.com/1686756626/Teaching-System/blob/main/AGENTS.md)。
前端展示网站见 Teaching-Dashboard 仓库。

## 关键文件

| 文件 | 说明 |
|------|------|
| `AGENTS.md` | 本文件 — AI 交接手册 |
| `README.md` | 人类可读总览，含目录结构和课表 |
| `备课规范.md` | 材料生成的通用规则（原卷/解析/教案/要点） |

## 目录结构（严格遵守）

```
{学生名}/
├── 学生/{周次}/
│   ├── {编号}-{科目}-{内容}（原卷）.md     ← 打印给学生，不含答案
│   └── {编号}-{科目}-{内容}要点.md         ← 打印给学生，知识要点
└── 教师/{周次}/
    ├── {编号}-{科目}-{内容}（解析）.md     ← 教师参考，含答案
    └── {编号}-{科目}-{内容}（教案）.md     ← 逐字稿，打开就能上课
```

### 路径层级（固定 4 层，不可增减）

| 层 | 内容 | 示例 |
|----|------|------|
| 第1层 | 学生姓名 | `黄涵松` |
| 第2层 | `学生` 或 `教师` | `学生` |
| 第3层 | 两位数字周次 | `08` |
| 第4层 | 文件名 | `01-语文-语法小测（原卷）.md` |

> ⚠️ **前端网站 Teaching-Dashboard 的 `parseMaterialsTree()` 函数依赖此结构（`parts.length === 4`）。**
> 任何改动都会导致页面显示异常。这是一条全局红线，记录在 Teaching-System/AGENTS.md 中。

### 文件命名格式

```
{编号}-{科目}-{内容}（类型）.md
```

| 元素 | 说明 | 示例 |
|------|------|------|
| 编号 | 两位数 01-99，按上课顺序 | 01、02、03 |
| 科目 | `语文` / `道法` / `历史` | 注意简称，不是"道德与法治" |
| 内容 | 具体知识点 | 语法小测、社会规则要点 |
| 类型 | `（原卷）`/`（解析）`/`（教案）`/ 无后缀=要点 | |

### ❌ 严禁写法

```
学生/黄涵松/08/...          ← 学生名不能在第2层
黄涵松/学生/第8周/...        ← 周次必须纯数字，不加"第""周"
黄涵松/学生/08/原卷/...      ← 不可增加第4层子文件夹
黄涵松/08/...                ← 不可省略角色层
```

## 材料生成规范

详细规范见 `备课规范.md`，简要版如下：

| 类型 | 核心要求 |
|------|----------|
| 原卷 | 绝不含答案，适合直接打印给学生 |
| 解析 | 每题含答案+解析+考点+评分标准 |
| 教案 | 逐字稿，精确到分钟，含教师原话和分支判断 |
| 要点 | 多用表格，给公式不给思路，附速记口诀 |

## 每次备课后的必须操作

1. 将所有生成的 `.md` 文件写入正确路径
2. 更新 `README.md` 中的目录结构和课表
3. 更新 `Student-Profiles` 中的学生档案和周次进度
4. 输出打印清单给教师确认

## 注意事项

- 分支为 `master`（非 main）— **这是全局红线**
- 所有文件为 Markdown 格式
- 科目用简称：`道法`（不是"道德与法治"）、`政治`（不是"思想政治"）

## 变更记录

- `[2026-04-03] 杨紫泠第8周语文教学材料（期中备考冲刺）`：新增学生材料3份（古诗文基础小测原卷、散文阅读专项练习原卷、答题方法速查卡）+ 教师材料3份（期中备考冲刺教案、古诗文基础小测解析、散文阅读专项练习解析）。重点突破四大薄弱点：人物形象动态分析、词语赏析四步法、环境描写背氛推烘、说明顺序判断。
- `[2026-04-03] 初始版本`
